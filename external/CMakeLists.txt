if(INSTALL_BOOST_INCLUDES)
	INSTALL(DIRECTORY ${CMAKE_CURRENT_SOURCE_DIR}/../boost DESTINATION include FILES_MATCHING PATTERN "*.hpp")
	INSTALL(DIRECTORY ${CMAKE_CURRENT_SOURCE_DIR}/../boost DESTINATION include FILES_MATCHING PATTERN "*.h")
else()
	add_subdirectory(../libs/ boost)
endif()
